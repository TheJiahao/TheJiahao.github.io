---
import HomeLayout from "../../layouts/HomeLayout.astro";
import { languageCodes } from "../../utils/translation";

export function getStaticPaths() {
    return languageCodes.map((language) => ({
        params: { language },
    }));
}
---

<HomeLayout />
