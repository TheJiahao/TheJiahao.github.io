---
import HomeLayout from "../../layouts/HomeLayout.astro";
import { locales } from "../../utils/translation";

export function getStaticPaths() {
    return locales.map((language) => ({
        params: { language },
    }));
}

const { language } = Astro.params;
---

<HomeLayout language={language} />
