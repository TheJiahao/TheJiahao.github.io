import LanguageButton from "./LanguageButton";

const ToolBar = () => {
    return (
        <aside card mx-auto p-2>
            <LanguageButton />
        </aside>
    );
};

export default ToolBar;
