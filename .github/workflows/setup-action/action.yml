name: Setup
description: Set up runtime

runs:
  using: composite
  steps:
    - name: Install Bun
      uses: oven-sh/setup-bun@v2

    - name: Install dependencies
      run: bun install
      shell: bash
